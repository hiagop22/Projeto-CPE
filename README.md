A project written in c++ which emulates a contact book. 
It options to create, list, search, delete, and update contacts. 
All informations are saved into a binary file.

#Running:
```shell
$ g++ main.cpp
$ ./a.out
```